/** @type {import('next').NextConfig} */
const nextConfig = {
  // DO NOT set output: 'export'
  reactStrictMode: true,
  experimental: {
    // optional, but fine
    // typedRoutes: true,
  },
};
module.exports = nextConfig;
